VITE_API_URL=
VITE_BASE_PATH=/
# Temporary, while production is still under testing - shows the seeded
# demo-account quick-login buttons on the live login page. Set to false (or
# remove the line) and rebuild once testing is done; see Login.tsx for why
# this is a flag rather than deleted code.
VITE_SHOW_DEMO_LOGIN=true
